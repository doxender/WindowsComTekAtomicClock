<ui:FluentWindow x:Class="ComTekAtomicClock.UI.Dialogs.ThemesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        xmlns:vm="clr-namespace:ComTekAtomicClock.UI.ViewModels"
        Title="Themes"
        Icon="pack://application:,,,/Assets/AppIcon.ico"
        Width="820" Height="720"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <DockPanel>
        <ui:TitleBar DockPanel.Dock="Top"
                     Title="Themes"
                     ShowMaximize="False"
                     ShowMinimize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/AppIcon.ico"/>
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <!--
          Header / instruction line, then a scrollable 3-column grid of
          theme tiles. Each tile is a Button styled to look like a card
          (no chrome) so the click + accessibility behavior comes for
          free. ThemeTile_Click reads the bound ThemePreview off
          DataContext, applies Theme to the tab the dialog was opened
          for, and closes with DialogResult=true.
        -->
        <Grid Margin="20,16,20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       FontFamily="Segoe UI Variable, Segoe UI, sans-serif"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="#FFB000"
                       Text="Themes"/>
            <TextBlock x:Name="EditingTabLabel" Grid.Row="1"
                       FontFamily="Segoe UI Variable, Segoe UI, sans-serif"
                       FontSize="11"
                       Foreground="#A8A39A"
                       Margin="0,2,0,12"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{x:Static vm:ThemeCatalog.All}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ThemePreview}">
                            <Button Click="ThemeTile_Click"
                                    Margin="6"
                                    Padding="8"
                                    Cursor="Hand"
                                    Background="#141414"
                                    BorderBrush="#3a3a3a"
                                    BorderThickness="1"
                                    HorizontalContentAlignment="Stretch"
                                    VerticalContentAlignment="Stretch"
                                    ToolTip="{Binding DisplayName}">
                                <StackPanel>
                                    <!--
                                      SharpVectors SvgViewbox renders the
                                      SVG at the requested box. Source
                                      binds to the ThemePreview.SvgUri
                                      pack-URI; the underlying SVG is
                                      embedded as a Resource (see
                                      ComTekAtomicClock.UI.csproj).
                                    -->
                                    <svgc:SvgViewbox Source="{Binding SvgUri}"
                                                     Width="220" Height="220"
                                                     HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding DisplayName}"
                                               FontFamily="Segoe UI Variable, Segoe UI, sans-serif"
                                               FontSize="13" FontWeight="SemiBold"
                                               Foreground="#FFB000"
                                               HorizontalAlignment="Center"
                                               Margin="0,8,0,2"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,12,0,0">
                <Button Content="Close"
                        Width="100" Height="30"
                        IsCancel="True"
                        Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</ui:FluentWindow>
